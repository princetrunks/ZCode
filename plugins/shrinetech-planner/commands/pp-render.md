---
name: pp-render
description: Render a MiniMax-H3 scene of Danielle or Charlie through the Overseer (render_h3_scene). Usage: /pp-render <subject>: <shot>
---
$ARGUMENTS
Call `render_h3_scene` once with the subject and the shot written like a director (framing, lens, setting, wardrobe verbatim, action, expressions, any dialogue as <d>[English] …</d>). Default tier is final; use `quick` only if the user says draft or test. Report the start summary; the clip lands in the Lily media gallery. Never queue a second render while one runs.
