---
name: pp-status
description: Planner snapshot — today's priorities, active projects and anything blocked, via the shrinetech-planner tools.
---
Use the shrinetech-planner tools: call `get_priorities`, then `list_projects` (active only), then `list_tasks` with status `blocked`.
Report in three short sections: Today (top 5 priorities with ticket ids), Active projects (name and open task count), Blocked (ticket, one line why).
Do not change anything. $ARGUMENTS
